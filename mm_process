#!/usr/bin/perl

use Mail::Miner;
use MIME::Parser;
$Mail::Miner::Message::DEBUG=1;

my $parser = new MIME::Parser;
$parser->output_to_core(1);

my $entity = $parser->parse(\*STDIN);
$entity = Mail::Miner::Message::process($entity);
print $entity->as_string;

